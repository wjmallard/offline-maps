# Credentials for the offline-maps build tools. Copy to .env (which is git-ignored).

# Free Mapillary API token, used by offline-maps-sync-photos to fetch the ~480
# Mapillary-referenced photos. Create one at:
#   https://www.mapillary.com/dashboard/developers  (register an app -> Client Token)
MAPILLARY_TOKEN=
